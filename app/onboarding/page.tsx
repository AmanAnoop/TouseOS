import { OnboardingWizard } from "@/components/onboarding/onboarding-wizard";

export const metadata = { title: "Get started — TouseOS" };

export default function OnboardingPage() {
  return <OnboardingWizard />;
}
